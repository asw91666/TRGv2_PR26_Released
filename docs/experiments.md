# Train

If you want to train the TRGv2 model on the ARKitFace dataset, use this command: `run_train.sh`

# Test

You can evaluate the TRG model which is trained on ARKitFace dataset on ARKitFace test dataset: `eval.sh`